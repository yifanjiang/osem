.container-fluid
  .row-fluid
    .span3
      = render 'admin/conference/sidebar', :activated => "Roles"
    .span9
      = semantic_form_for(@conference, :url => admin_conference_roles_update_path(@conference.short_title)) do |f|

        = dynamic_association :roles, "Roles", f
        = f.action :submit, :as => :button, :button_html => {:class => "btn btn-primary"}